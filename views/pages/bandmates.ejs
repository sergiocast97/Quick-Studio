<div class="index_page">

    <!-- Sidenav -->
    <%- include('../components/main/sidebar', { user: user }) %>

    <div class="page">

        <!-- Top Bar -->
        <%- include('../components/main/topbar', { page_title: title }) %>

        <!-- Main Area -->
        <div class="bandmates_area">
            <div class="bandmates">
                <% for(bandmate of bandmate_list){ %>
                    <%- include('../components/bandmate_item', { bandmate: bandmate }) %>
                <% } %>
            </div>
        </div>
    </div>
</div>